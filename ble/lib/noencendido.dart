import 'package:flutter/material.dart';

class NoEncendido extends StatelessWidget {
  const NoEncendido({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
